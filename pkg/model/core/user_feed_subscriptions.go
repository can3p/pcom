// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package core

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserFeedSubscription is an object representing the database table.
type UserFeedSubscription struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID    string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	FeedID    string    `boil:"feed_id" json:"feed_id" toml:"feed_id" yaml:"feed_id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *userFeedSubscriptionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userFeedSubscriptionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserFeedSubscriptionColumns = struct {
	ID        string
	UserID    string
	FeedID    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	FeedID:    "feed_id",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

var UserFeedSubscriptionTableColumns = struct {
	ID        string
	UserID    string
	FeedID    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "user_feed_subscriptions.id",
	UserID:    "user_feed_subscriptions.user_id",
	FeedID:    "user_feed_subscriptions.feed_id",
	CreatedAt: "user_feed_subscriptions.created_at",
	UpdatedAt: "user_feed_subscriptions.updated_at",
}

// Generated where

var UserFeedSubscriptionWhere = struct {
	ID        whereHelperstring
	UserID    whereHelperstring
	FeedID    whereHelperstring
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"user_feed_subscriptions\".\"id\""},
	UserID:    whereHelperstring{field: "\"user_feed_subscriptions\".\"user_id\""},
	FeedID:    whereHelperstring{field: "\"user_feed_subscriptions\".\"feed_id\""},
	CreatedAt: whereHelpertime_Time{field: "\"user_feed_subscriptions\".\"created_at\""},
	UpdatedAt: whereHelpertime_Time{field: "\"user_feed_subscriptions\".\"updated_at\""},
}

// UserFeedSubscriptionRels is where relationship names are stored.
var UserFeedSubscriptionRels = struct {
	Feed string
	User string
}{
	Feed: "Feed",
	User: "User",
}

// userFeedSubscriptionR is where relationships are stored.
type userFeedSubscriptionR struct {
	Feed *RSSFeed `boil:"Feed" json:"Feed" toml:"Feed" yaml:"Feed"`
	User *User    `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userFeedSubscriptionR) NewStruct() *userFeedSubscriptionR {
	return &userFeedSubscriptionR{}
}

func (r *userFeedSubscriptionR) GetFeed() *RSSFeed {
	if r == nil {
		return nil
	}
	return r.Feed
}

func (r *userFeedSubscriptionR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// userFeedSubscriptionL is where Load methods for each relationship are stored.
type userFeedSubscriptionL struct{}

var (
	userFeedSubscriptionAllColumns            = []string{"id", "user_id", "feed_id", "created_at", "updated_at"}
	userFeedSubscriptionColumnsWithoutDefault = []string{"id", "user_id", "feed_id", "created_at", "updated_at"}
	userFeedSubscriptionColumnsWithDefault    = []string{}
	userFeedSubscriptionPrimaryKeyColumns     = []string{"id"}
	userFeedSubscriptionGeneratedColumns      = []string{}
)

type (
	// UserFeedSubscriptionSlice is an alias for a slice of pointers to UserFeedSubscription.
	// This should almost always be used instead of []UserFeedSubscription.
	UserFeedSubscriptionSlice []*UserFeedSubscription

	userFeedSubscriptionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userFeedSubscriptionType                 = reflect.TypeOf(&UserFeedSubscription{})
	userFeedSubscriptionMapping              = queries.MakeStructMapping(userFeedSubscriptionType)
	userFeedSubscriptionPrimaryKeyMapping, _ = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, userFeedSubscriptionPrimaryKeyColumns)
	userFeedSubscriptionInsertCacheMut       sync.RWMutex
	userFeedSubscriptionInsertCache          = make(map[string]insertCache)
	userFeedSubscriptionUpdateCacheMut       sync.RWMutex
	userFeedSubscriptionUpdateCache          = make(map[string]updateCache)
	userFeedSubscriptionUpsertCacheMut       sync.RWMutex
	userFeedSubscriptionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneP returns a single userFeedSubscription record from the query, and panics on error.
func (q userFeedSubscriptionQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *UserFeedSubscription {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single userFeedSubscription record from the query.
func (q userFeedSubscriptionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserFeedSubscription, error) {
	o := &UserFeedSubscription{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "core: failed to execute a one query for user_feed_subscriptions")
	}

	return o, nil
}

// AllP returns all UserFeedSubscription records from the query, and panics on error.
func (q userFeedSubscriptionQuery) AllP(ctx context.Context, exec boil.ContextExecutor) UserFeedSubscriptionSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all UserFeedSubscription records from the query.
func (q userFeedSubscriptionQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserFeedSubscriptionSlice, error) {
	var o []*UserFeedSubscription

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "core: failed to assign all query results to UserFeedSubscription slice")
	}

	return o, nil
}

// CountP returns the count of all UserFeedSubscription records in the query, and panics on error.
func (q userFeedSubscriptionQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all UserFeedSubscription records in the query.
func (q userFeedSubscriptionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "core: failed to count user_feed_subscriptions rows")
	}

	return count, nil
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q userFeedSubscriptionQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q userFeedSubscriptionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "core: failed to check if user_feed_subscriptions exists")
	}

	return count > 0, nil
}

// Feed pointed to by the foreign key.
func (o *UserFeedSubscription) Feed(mods ...qm.QueryMod) rssFeedQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.FeedID),
	}

	queryMods = append(queryMods, mods...)

	return RSSFeeds(queryMods...)
}

// User pointed to by the foreign key.
func (o *UserFeedSubscription) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadFeed allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userFeedSubscriptionL) LoadFeed(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserFeedSubscription interface{}, mods queries.Applicator) error {
	var slice []*UserFeedSubscription
	var object *UserFeedSubscription

	if singular {
		var ok bool
		object, ok = maybeUserFeedSubscription.(*UserFeedSubscription)
		if !ok {
			object = new(UserFeedSubscription)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserFeedSubscription)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserFeedSubscription))
			}
		}
	} else {
		s, ok := maybeUserFeedSubscription.(*[]*UserFeedSubscription)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserFeedSubscription)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserFeedSubscription))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &userFeedSubscriptionR{}
		}
		args[object.FeedID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userFeedSubscriptionR{}
			}

			args[obj.FeedID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`rss_feeds`),
		qm.WhereIn(`rss_feeds.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RSSFeed")
	}

	var resultSlice []*RSSFeed
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RSSFeed")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for rss_feeds")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for rss_feeds")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Feed = foreign
		if foreign.R == nil {
			foreign.R = &rssFeedR{}
		}
		foreign.R.FeedUserFeedSubscriptions = append(foreign.R.FeedUserFeedSubscriptions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.FeedID == foreign.ID {
				local.R.Feed = foreign
				if foreign.R == nil {
					foreign.R = &rssFeedR{}
				}
				foreign.R.FeedUserFeedSubscriptions = append(foreign.R.FeedUserFeedSubscriptions, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userFeedSubscriptionL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserFeedSubscription interface{}, mods queries.Applicator) error {
	var slice []*UserFeedSubscription
	var object *UserFeedSubscription

	if singular {
		var ok bool
		object, ok = maybeUserFeedSubscription.(*UserFeedSubscription)
		if !ok {
			object = new(UserFeedSubscription)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserFeedSubscription)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserFeedSubscription))
			}
		}
	} else {
		s, ok := maybeUserFeedSubscription.(*[]*UserFeedSubscription)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserFeedSubscription)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserFeedSubscription))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &userFeedSubscriptionR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userFeedSubscriptionR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserFeedSubscriptions = append(foreign.R.UserFeedSubscriptions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserFeedSubscriptions = append(foreign.R.UserFeedSubscriptions, local)
				break
			}
		}
	}

	return nil
}

// SetFeedP of the userFeedSubscription to the related item.
// Sets o.R.Feed to related.
// Adds o to related.R.FeedUserFeedSubscriptions.
// Panics on error.
func (o *UserFeedSubscription) SetFeedP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RSSFeed) {
	if err := o.SetFeed(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetFeed of the userFeedSubscription to the related item.
// Sets o.R.Feed to related.
// Adds o to related.R.FeedUserFeedSubscriptions.
func (o *UserFeedSubscription) SetFeed(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RSSFeed) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_feed_subscriptions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"feed_id"}),
		strmangle.WhereClause("\"", "\"", 2, userFeedSubscriptionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.FeedID = related.ID
	if o.R == nil {
		o.R = &userFeedSubscriptionR{
			Feed: related,
		}
	} else {
		o.R.Feed = related
	}

	if related.R == nil {
		related.R = &rssFeedR{
			FeedUserFeedSubscriptions: UserFeedSubscriptionSlice{o},
		}
	} else {
		related.R.FeedUserFeedSubscriptions = append(related.R.FeedUserFeedSubscriptions, o)
	}

	return nil
}

// SetUserP of the userFeedSubscription to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserFeedSubscriptions.
// Panics on error.
func (o *UserFeedSubscription) SetUserP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) {
	if err := o.SetUser(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetUser of the userFeedSubscription to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserFeedSubscriptions.
func (o *UserFeedSubscription) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_feed_subscriptions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userFeedSubscriptionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userFeedSubscriptionR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserFeedSubscriptions: UserFeedSubscriptionSlice{o},
		}
	} else {
		related.R.UserFeedSubscriptions = append(related.R.UserFeedSubscriptions, o)
	}

	return nil
}

// UserFeedSubscriptions retrieves all the records using an executor.
func UserFeedSubscriptions(mods ...qm.QueryMod) userFeedSubscriptionQuery {
	mods = append(mods, qm.From("\"user_feed_subscriptions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_feed_subscriptions\".*"})
	}

	return userFeedSubscriptionQuery{q}
}

// FindUserFeedSubscriptionP retrieves a single record by ID with an executor, and panics on error.
func FindUserFeedSubscriptionP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *UserFeedSubscription {
	retobj, err := FindUserFeedSubscription(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindUserFeedSubscription retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserFeedSubscription(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*UserFeedSubscription, error) {
	userFeedSubscriptionObj := &UserFeedSubscription{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_feed_subscriptions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userFeedSubscriptionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "core: unable to select from user_feed_subscriptions")
	}

	return userFeedSubscriptionObj, nil
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *UserFeedSubscription) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserFeedSubscription) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("core: no user_feed_subscriptions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(userFeedSubscriptionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userFeedSubscriptionInsertCacheMut.RLock()
	cache, cached := userFeedSubscriptionInsertCache[key]
	userFeedSubscriptionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userFeedSubscriptionAllColumns,
			userFeedSubscriptionColumnsWithDefault,
			userFeedSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_feed_subscriptions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_feed_subscriptions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "core: unable to insert into user_feed_subscriptions")
	}

	if !cached {
		userFeedSubscriptionInsertCacheMut.Lock()
		userFeedSubscriptionInsertCache[key] = cache
		userFeedSubscriptionInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateP uses an executor to update the UserFeedSubscription, and panics on error.
// See Update for more documentation.
func (o *UserFeedSubscription) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the UserFeedSubscription.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserFeedSubscription) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	userFeedSubscriptionUpdateCacheMut.RLock()
	cache, cached := userFeedSubscriptionUpdateCache[key]
	userFeedSubscriptionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userFeedSubscriptionAllColumns,
			userFeedSubscriptionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("core: unable to update user_feed_subscriptions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_feed_subscriptions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userFeedSubscriptionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, append(wl, userFeedSubscriptionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to update user_feed_subscriptions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: failed to get rows affected by update for user_feed_subscriptions")
	}

	if !cached {
		userFeedSubscriptionUpdateCacheMut.Lock()
		userFeedSubscriptionUpdateCache[key] = cache
		userFeedSubscriptionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q userFeedSubscriptionQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q userFeedSubscriptionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to update all for user_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to retrieve rows affected for user_feed_subscriptions")
	}

	return rowsAff, nil
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o UserFeedSubscriptionSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserFeedSubscriptionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("core: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_feed_subscriptions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userFeedSubscriptionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to update all in userFeedSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to retrieve rows affected all in update all userFeedSubscription")
	}
	return rowsAff, nil
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *UserFeedSubscription) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserFeedSubscription) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("core: no user_feed_subscriptions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(userFeedSubscriptionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userFeedSubscriptionUpsertCacheMut.RLock()
	cache, cached := userFeedSubscriptionUpsertCache[key]
	userFeedSubscriptionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userFeedSubscriptionAllColumns,
			userFeedSubscriptionColumnsWithDefault,
			userFeedSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userFeedSubscriptionAllColumns,
			userFeedSubscriptionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("core: unable to upsert user_feed_subscriptions, could not build update column list")
		}

		ret := strmangle.SetComplement(userFeedSubscriptionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userFeedSubscriptionPrimaryKeyColumns) == 0 {
				return errors.New("core: unable to upsert user_feed_subscriptions, could not build conflict column list")
			}

			conflict = make([]string, len(userFeedSubscriptionPrimaryKeyColumns))
			copy(conflict, userFeedSubscriptionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_feed_subscriptions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userFeedSubscriptionType, userFeedSubscriptionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "core: unable to upsert user_feed_subscriptions")
	}

	if !cached {
		userFeedSubscriptionUpsertCacheMut.Lock()
		userFeedSubscriptionUpsertCache[key] = cache
		userFeedSubscriptionUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single UserFeedSubscription record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *UserFeedSubscription) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single UserFeedSubscription record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserFeedSubscription) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("core: no UserFeedSubscription provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userFeedSubscriptionPrimaryKeyMapping)
	sql := "DELETE FROM \"user_feed_subscriptions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to delete from user_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: failed to get rows affected by delete for user_feed_subscriptions")
	}

	return rowsAff, nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q userFeedSubscriptionQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q userFeedSubscriptionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("core: no userFeedSubscriptionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to delete all from user_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: failed to get rows affected by deleteall for user_feed_subscriptions")
	}

	return rowsAff, nil
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o UserFeedSubscriptionSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserFeedSubscriptionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_feed_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userFeedSubscriptionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "core: unable to delete all from userFeedSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "core: failed to get rows affected by deleteall for user_feed_subscriptions")
	}

	return rowsAff, nil
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *UserFeedSubscription) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserFeedSubscription) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserFeedSubscription(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *UserFeedSubscriptionSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserFeedSubscriptionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserFeedSubscriptionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_feed_subscriptions\".* FROM \"user_feed_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userFeedSubscriptionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "core: unable to reload all in UserFeedSubscriptionSlice")
	}

	*o = slice

	return nil
}

// UserFeedSubscriptionExistsP checks if the UserFeedSubscription row exists. Panics on error.
func UserFeedSubscriptionExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := UserFeedSubscriptionExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// UserFeedSubscriptionExists checks if the UserFeedSubscription row exists.
func UserFeedSubscriptionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_feed_subscriptions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "core: unable to check if user_feed_subscriptions exists")
	}

	return exists, nil
}

// Exists checks if the UserFeedSubscription row exists.
func (o *UserFeedSubscription) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserFeedSubscriptionExists(ctx, exec, o.ID)
}
